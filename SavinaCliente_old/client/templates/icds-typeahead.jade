.form-group
  .input-group
    span.input-group-addon
      i.fa.fa-fw(ng-class='typeahead.fetching && "fa-circle-o-notch fa-spin" || "fa-search"')

    input.form-control(
      ng-keypress='$event.keyCode === 13 && $event.preventDefault()',
      placeholder='Ingresa el código o descripción CIE-10')

    span.input-group-btn
      button.btn.btn-default(
        ng-disabled='typeahead.fetching',
        ng-click='typeahead.clear()',
        type='button')

        i.fa.fa-fw.fa-undo

.panel-body.margin-bottom-base(ng-if='typeahead.fetched && typeahead.results.length')
  i.fa.fa-info-circle.margin-right-small
  | Selecciona uno de los resultados para agregarlo a la lista

.panel-body.margin-bottom-base(ng-if='typeahead.fetched && !typeahead.results.length')
  i.fa.fa-info-circle.margin-right-small
  | No hay resultados para tu búsqueda

.panel-body.margin-bottom-base(ng-if='typeahead.error === 400')
  i.fa.fa-exclamation-circle.margin-right-small
  | Al parecer ese filtro no es permitido

.panel-body.margin-bottom-base(ng-if='typeahead.error === 500')
  i.fa.fa-exclamation-circle.margin-right-small
  | Ocurrió un problema al solicitar los resultados

.panel-body.margin-bottom-base(ng-if='!typeahead.fetched')
  i.fa.fa-info-circle.margin-right-small
  | Ingresa tu filtro en el campo superior para ver resultados

button.form-group(
  ng-repeat='icd in typeahead.results track by icd.id',
  ng-click='icds.add(icd); typeahead.clear(true)',
  type='button')

  .input-group
    .input-group-addon
      i.fa.fa-fw.fa-chevron-circle-left

    .form-control.btn-default
      strong.margin-right-base {{ icd.code }}
      | {{ icd.description }}

    .input-group-addon
      i.fa.fa-fw.fa-plus
